export const FIRST_DOZEN = Array.from({ length: 12 }, (_, i) => i + 1);
export const SECOND_DOZEN = Array.from({ length: 12 }, (_, i) => i + 13);
export const THIRD_DOZEN = Array.from({ length: 12 }, (_, i) => i + 25);
